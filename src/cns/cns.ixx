module;

/// @brief Chris Nichols Software (cns) library
export module cns;

export import cns.math;
export import cns.format;

/// @brief Contains all types and functions in the cns module
namespace cns {}